{
  "plugin_id": 65055,
  "finding_name": "Jenkins < 1.498 / 1.480.2 and Jenkins Enterprise 1.447.x / 1.466.x < 1.447.6.1 / 1.466.12.1 Unspecified Master Cryptographic Key Information Disclosure",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -m 20 -I https://{host}:{port}/",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 -I http://{host}:{port}/",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "jenkins"
    ],
    "version_patterns": [
      "X-Jenkins: ([0-9]+\\.[0-9]+(?:\\.[0-9]+)?)",
      "x-jenkins: ([0-9]+\\.[0-9]+(?:\\.[0-9]+)?)",
      "Jenkins/([0-9]+\\.[0-9]+(?:\\.[0-9]+)?)",
      "jenkins/([0-9]+\\.[0-9]+(?:\\.[0-9]+)?)"
    ],
    "affected_lt": "1.498",
    "fixed_version": "1.498"
  },
  "analyst_note": "Jenkins exposes its version in the X-Jenkins HTTP response header. Fixed: 1.498. CVEs: CVE-2013-0158.",
  "references": [
    "https://nvd.nist.gov/vuln/detail/CVE-2013-0158",
    "http://www.nessus.org/u?0f8bc6d8",
    "http://www.nessus.org/u?bd73e7b2"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16"
}