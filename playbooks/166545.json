{
  "plugin_id": 166545,
  "finding_name": "nginx ngx_http_mp4_module Memory Disclosure (CVE-2022-41742)",
  "service": "banner",
  "command_template": "nmap -Pn -sV --version-light -p {port} {host}",
  "timeout_seconds": 30,
  "allowed_ports": [],
  "blocked_ports": [],
  "starttls_protocol_map": {},
  "fallback_commands": [
    "curl -k -s -I --silent --show-error --max-time 15 https://{host}:{port}/",
    "curl -s -I --silent --show-error --max-time 15 http://{host}:{port}/"
  ],
  "version_rule": {
    "product_terms": [
      "nginx"
    ],
    "version_patterns": [
      "nginx/([0-9][0-9a-zA-Z._-]*)",
      "server: nginx/([0-9][0-9a-zA-Z._-]*)"
    ],
    "affected_lt": "1.22.1",
    "fixed_version": "1.22.1"
  },
  "validated_if": [],
  "validated_if_absent": [],
  "not_validated_if": [],
  "not_validated_if_present": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "operation not permitted"
  ],
  "failure_reason_map": {
    "failed to resolve": "host_down",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "operation not permitted": "inconclusive"
  },
  "references": [
    "https://nginx.org/en/CHANGES",
    "https://nvd.nist.gov/vuln/detail/CVE-2022-41742"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16",
  "fallback_commands_json": [
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ]
}
