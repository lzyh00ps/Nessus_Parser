{
  "plugin_id": 106395,
  "finding_name": "IBM WebSphere Portal 8.5.0.x < 8.5.0.0 CF15 Unspecified XSS (PI92039)",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -m 20 -I https://{host}:{port}/",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 -I http://{host}:{port}/",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "websphere",
      "ibm websphere"
    ],
    "version_patterns": [
      "IBM_HTTP_Server/([0-9]+\\.[0-9]+\\.[0-9.]+)",
      "ibm_http_server/([0-9]+\\.[0-9]+\\.[0-9.]+)",
      "WebSphere Application Server[/ ]([0-9]+\\.[0-9]+\\.[0-9.]+)",
      "websphere application server[/ ]([0-9]+\\.[0-9]+\\.[0-9.]+)",
      "IBM WebSphere[/ ]([0-9]+\\.[0-9]+\\.[0-9.]+)"
    ],
    "affected_lt": "8.5.0.0",
    "fixed_version": "8.5.0.0"
  },
  "analyst_note": "IBM WebSphere version in IBM_HTTP_Server response header or admin console. Fixed: 8.5.0.0. CVEs: CVE-2018-1361.",
  "references": [
    "https://nvd.nist.gov/vuln/detail/CVE-2018-1361",
    "http://www-01.ibm.com/support/docview.wss?uid=swg22012409"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16"
}