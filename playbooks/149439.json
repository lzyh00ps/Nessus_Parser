{
  "plugin_id": 149439,
  "finding_name": "Atlassian Confluence < 7.11.0 SSRF (CONFSERVER-61453)",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -m 20 https://{host}:{port}/login.action",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 http://{host}:{port}/login.action",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "confluence"
    ],
    "version_patterns": [
      "ajs-version-number.*content=\"([0-9]+\\.[0-9]+\\.[0-9]+)\"",
      "X-Confluence-Version: ([0-9]+\\.[0-9]+\\.[0-9]+)",
      "x-confluence-version: ([0-9]+\\.[0-9]+\\.[0-9]+)",
      "confluence[/ ]([0-9]+\\.[0-9]+\\.[0-9]+)"
    ],
    "affected_lt": "7.11.0",
    "fixed_version": "7.11.0"
  },
  "analyst_note": "Confluence version in login page meta tag ajs-version-number. Fixed: 7.11.0. CVEs: CVE-2020-29445.",
  "references": [
    "https://nvd.nist.gov/vuln/detail/CVE-2020-29445",
    "https://jira.atlassian.com/browse/CONFSERVER-61453"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16"
}