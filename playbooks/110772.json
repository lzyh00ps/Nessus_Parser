{
  "plugin_id": 110772,
  "finding_name": "Atlassian Crowd < 2.11.2 OAuth Plugin IconUriServlet Internal Network Resource Disclosure CSRF",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -m 20 https://{host}:{port}/crowd/",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 http://{host}:{port}/crowd/",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "crowd",
      "atlassian crowd"
    ],
    "version_patterns": [
      "Atlassian Crowd[/ ]([0-9]+\\.[0-9]+\\.[0-9]+)",
      "crowd[/ ]([0-9]+\\.[0-9]+\\.[0-9]+)",
      "Crowd[/ ]([0-9]+\\.[0-9]+\\.[0-9]+)"
    ],
    "affected_lt": "2.11.2",
    "fixed_version": "2.11.2"
  },
  "analyst_note": "Atlassian Crowd version in management web interface. Fixed: 2.11.2. CVEs: CVE-2017-9506.",
  "references": [
    "https://nvd.nist.gov/vuln/detail/CVE-2017-9506",
    "https://ecosystem.atlassian.net/browse/OAUTH-344"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16"
}