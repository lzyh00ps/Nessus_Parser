{
  "plugin_id": 144290,
  "finding_name": "IBM HTTP Server 8.5.0.0 <= 8.5.5.1 / 8.0.0.0 <= 8.0.0.8 / 7.0.0.0 <= 7.0.0.31 / 6.1.0.0 <= 6.1.0.47 (242057)",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -m 20 -I https://{host}:{port}/",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 -I http://{host}:{port}/",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "ibm http server"
    ],
    "version_patterns": [
      "IBM_HTTP_Server/([0-9]+\\.[0-9]+\\.[0-9.]+)",
      "ibm_http_server/([0-9]+\\.[0-9]+\\.[0-9.]+)",
      "http-server-header: ibm_http_server/([0-9]+\\.[0-9]+\\.[0-9.]+)"
    ],
    "affected_lt": "8.5.0",
    "fixed_version": "8.5.0"
  },
  "analyst_note": "IBM HTTP Server version in Server HTTP response header. Fixed: 8.5.0. CVEs: CVE-2014-0098.",
  "references": [
    "https://nvd.nist.gov/vuln/detail/CVE-2014-0098",
    "https://www.ibm.com/support/pages/node/242057"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16"
}