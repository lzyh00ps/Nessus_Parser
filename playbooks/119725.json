{
  "plugin_id": 119725,
  "finding_name": "Squid 3.1.12.2 <= 3.1.x <= 3.1.23 / 3.2.0.8 <= 3.2.x <= 3.2.14 / 3.3.x / 3.4.x / 3.5.x <= 3.5.27 / 4.x < 4.0.13 Denial of Service Vulnerability (SQUID-2018:3)",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -m 20 -I https://{host}:{port}/",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 -I http://{host}:{port}/ --proxy http://{host}:{port}",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "squid"
    ],
    "version_patterns": [
      "squid/([0-9]+\\.[0-9]+\\.[0-9.]+)",
      "Squid/([0-9]+\\.[0-9]+\\.[0-9.]+)",
      "http-server-header: squid/([0-9]+\\.[0-9]+\\.[0-9.]+)"
    ],
    "affected_lt": "4.0.13",
    "fixed_version": "4.0.13"
  },
  "analyst_note": "Squid proxy version in Server or Via HTTP response header. Fixed: 4.0.13. CVEs: CVE-2018-1172.",
  "references": [
    "https://nvd.nist.gov/vuln/detail/CVE-2018-1172",
    "http://www.squid-cache.org/Advisories/SQUID-2018_3.txt"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16"
}