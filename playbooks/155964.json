{
  "plugin_id": 155964,
  "finding_name": "ThinkPHP < 5.0.24 Remote Code Execution",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -m 20 -I https://{host}:{port}/",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 -I http://{host}:{port}/",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [
    "ThinkPHP",
    "thinkphp"
  ],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "thinkphp"
    ],
    "version_patterns": [
      "ThinkPHP[/ ]([0-9]+\\.[0-9]+\\.[0-9.]+)",
      "thinkphp[/ ]([0-9]+\\.[0-9]+\\.[0-9.]+)"
    ],
    "affected_lt": "5.0.24",
    "fixed_version": "5.0.24"
  },
  "analyst_note": "ThinkPHP 5.x < 5.0.24 RCE via uncontrolled method invocation in the routing component. Actively exploited in Asia-Pacific region. ThinkPHP error pages expose the framework version. If ThinkPHP is detected via error pages or X-Powered-By header, treat as high priority for version verification. Fixed: 5.0.24.",
  "references": [
    "https://nvd.nist.gov/vuln/detail/CVE-2018-20062",
    "https://github.com/SkyBlueEternal/thinkphp-RCE-POC-Collection",
    "https://www.exploit-db.com/exploits/46101"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16"
}