{
  "plugin_id": 107262,
  "finding_name": "nginx < 0.7.64 / 0.8.x < 0.8.23 Multiple Vulnerabilities",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -m 20 -I https://{host}:{port}/",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 -I http://{host}:{port}/",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "nginx"
    ],
    "version_patterns": [
      "Server: nginx/([0-9]+\\.[0-9]+\\.[0-9]+)",
      "server: nginx/([0-9]+\\.[0-9]+\\.[0-9]+)",
      "nginx/([0-9]+\\.[0-9]+\\.[0-9]+)",
      "http-server-header: nginx/([0-9]+\\.[0-9]+\\.[0-9]+)"
    ],
    "affected_lt": "0.7.64",
    "fixed_version": "0.7.64"
  },
  "analyst_note": "nginx version exposed in the Server HTTP response header. Fixed: 0.7.64. CVEs: CVE-2009-3555, CVE-2009-4487.",
  "references": [
    "https://nvd.nist.gov/vuln/detail/CVE-2009-3555",
    "http://nginx.org/en/security_advisories.html"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16"
}