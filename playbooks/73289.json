{
  "plugin_id": 73289,
  "finding_name": "PHP < 5.4.28 / 5.5.12 Multiple Vulnerabilities",
  "service": "http",
  "port_logic": "any",
  "command_template": "curl -k -s -I --max-time 15 http://{host}:{port}/",
  "timeout_seconds": 30,
  "allowed_ports": [],
  "blocked_ports": [],
  "fallback_commands": [
    "curl -k -s -I --max-time 15 https://{host}:{port}/",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent": [],
  "not_validated_if": [],
  "not_validated_if_present": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "network is unreachable"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "php",
      "installed version",
      "x-powered-by"
    ],
    "version_patterns": [
      "installed version\\s*:\\s*([0-9]+\\.[0-9]+(?:\\.[0-9]+)?)",
      "x-powered-by: php/([0-9]+\\.[0-9]+(?:\\.[0-9]+)?)",
      "php/([0-9]+\\.[0-9]+(?:\\.[0-9]+)?)",
      "php\\s+([0-9]+\\.[0-9]+(?:\\.[0-9]+)?)"
    ],
    "fixed_version": "5.4.28 / 5.5.12"
  },
  "analyst_note": "Multi-branch fix: PHP 5.4.x fixed in 5.4.28, PHP 5.5.x fixed in 5.5.12.",
  "references": [],
  "reviewed_by": "analyst",
  "last_verified": "2026-04-06"
}
