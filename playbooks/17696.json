{
  "plugin_id": 17696,
  "finding_name": "Apache HTTP Server 403 Error Page UTF-7 Encoded XSS",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -m 20 -I https://{host}:{port}/",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 -I http://{host}:{port}/",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "apache",
      "apache httpd",
      "apache http server"
    ],
    "version_patterns": [
      "Server: Apache/([0-9]+\\.[0-9]+\\.[0-9]+)",
      "server: apache/([0-9]+\\.[0-9]+\\.[0-9]+)",
      "Apache/([0-9]+\\.[0-9]+\\.[0-9]+)",
      "apache/([0-9]+\\.[0-9]+\\.[0-9]+)",
      "http-server-header: apache/([0-9]+\\.[0-9]+\\.[0-9]+)"
    ]
  },
  "analyst_note": "Apache HTTP Server version in Server response header. CVEs: CVE-2008-2168.",
  "references": [
    "https://nvd.nist.gov/vuln/detail/CVE-2008-2168",
    "https://seclists.org/bugtraq/2008/May/109",
    "https://seclists.org/bugtraq/2008/May/166"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16"
}