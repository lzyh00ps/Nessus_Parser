{
  "plugin_id": 152853,
  "finding_name": "PHP < 7.3.28 Email Header Injection",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -I --max-time 15 http://{host}:{port}/",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 -I http://{host}:{port}/",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable"
  },
  "version_rule": {
    "product_terms": [
      "php",
      "installed version"
    ],
    "version_patterns": [
      "installed version\\s*:\\s*([0-9]+\\.[0-9]+\\.[0-9]+)",
      "x-powered-by: php/([0-9]+\\.[0-9]+\\.[0-9]+)",
      "php/([0-9]+\\.[0-9]+\\.[0-9]+)",
      "php\\s+([0-9]+\\.[0-9]+\\.[0-9]+)"
    ],
    "affected_lt": "7.3.28",
    "fixed_version": "7.3.28"
  },
  "analyst_note": "Version detection via HTTP banner/header. Fixed: 7.3.28.",
  "references": [
    "https://www.php.net/ChangeLog-7.php#7.3.28"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16",
  "fallback_commands": [
    "curl -k -s -I --max-time 15 https://{host}:{port}/",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ]
}