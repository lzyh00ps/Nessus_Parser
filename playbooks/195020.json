{
  "plugin_id": 195020,
  "finding_name": "GitLab 16.4.3 < 16.4.4 / 16.5.3 < 16.5.4 / 16.6.1 < 16.6.2 (CVE-2023-6564)",
  "service": "https",
  "port_logic": "any",
  "command_template": "curl -k -s -m 20 https://{host}:{port}/api/v4/version",
  "timeout_seconds": 30,
  "allowed_ports_json": "[]",
  "blocked_ports_json": "[]",
  "fallback_commands_json": [
    "curl -k -s -m 20 http://{host}:{port}/api/v4/version",
    "nmap -Pn --script http-headers,http-generator,http-server-header,http-title -p {port} {host}",
    "nmap -Pn -sV --version-light -p {port} {host}"
  ],
  "validated_if": [],
  "validated_if_absent_json": [],
  "not_validated_if": [],
  "not_validated_if_present_json": [],
  "inconclusive_if": [
    "failed to resolve",
    "0 hosts up",
    "host seems down",
    "timed out",
    "connection refused",
    "no route to host",
    "could not connect",
    "network is unreachable",
    "ssl handshake",
    "401 unauthorized",
    "403 forbidden"
  ],
  "failure_reason_map": {
    "failed to resolve": "dns_failure",
    "0 hosts up": "host_down",
    "host seems down": "host_down",
    "timed out": "port_filtered",
    "connection refused": "port_closed",
    "no route to host": "host_unreachable",
    "network is unreachable": "host_unreachable",
    "401 unauthorized": "auth_required",
    "403 forbidden": "auth_required"
  },
  "version_rule": {
    "product_terms": [
      "gitlab"
    ],
    "version_patterns": [
      "\"version\":\"([0-9]+\\.[0-9]+\\.[0-9]+)\"",
      "gitlab[/ ]([0-9]+\\.[0-9]+\\.[0-9]+)",
      "http-generator: gitlab ([0-9]+\\.[0-9]+\\.[0-9]+)"
    ],
    "affected_lt": "16.4.4",
    "fixed_version": "16.4.4"
  },
  "analyst_note": "GitLab version exposed via /api/v4/version JSON endpoint (may require auth on newer instances). Fixed: 16.4.4. CVEs: CVE-2023-6564.",
  "references": [
    "https://nvd.nist.gov/vuln/detail/CVE-2023-6564",
    "http://www.nessus.org/u?07032433",
    "https://gitlab.com/gitlab-com/gl-infra/production/-/issues/17213"
  ],
  "reviewed_by": "analyst",
  "last_verified": "2026-03-16"
}